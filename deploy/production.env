# Production Environment Variables
NODE_ENV=production
PORT=3000
WS_PORT=8080
HOST=0.0.0.0

# Database
DATABASE_PATH=/var/lib/geograph/geograph.db

# SSL (if using direct Node.js SSL, otherwise handled by Nginx)
# SSL_CERT_PATH=/etc/ssl/certs/geograph.crt
# SSL_KEY_PATH=/etc/ssl/private/geograph.key

# Performance settings for Nanode 1GB
MAX_CONCURRENT_GAMES=5
CLEANUP_INTERVAL_HOURS=6
MAX_MEMORY_RESTART=200M